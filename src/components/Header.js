import React, { Component } from "react";

/**
 * Header element, renders Header elements, actually it is hidden
 */
export default class Header extends Component {
  render() {
    return (
      <header className="App-header">
        <h1 className="App-title">BouncyBall</h1>
      </header>
    );
  }
}
